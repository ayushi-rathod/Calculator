<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
    
    <style name="FunctionalButtons">
        <item name="android:textSize">30sp</item>
        <item name="android:textColor">@color/funcButtonColorText</item>
        <item name="android:background">@drawable/funct_buttons</item>
        <item name="android:layout_margin">5sp</item>
        <item name="android:minHeight">75dp</item>
        <item name="android:minWidth">85dp</item>
    </style>

</resources>
